# 测试恢复按钮功能

## 测试步骤

1. **启动应用**
   ```bash
   npm run dev
   ```

2. **访问应用**
   打开 http://localhost:5173

3. **找到一个 Python 代码块**
   - 在任意章节找到 Python 代码块
   - 例如 BMI 计算示例

4. **进入编辑模式**
   - 点击"编辑"按钮
   - 工具栏会显示：`[复制] [查看] [恢复] [全屏] [运行]`

5. **修改代码**
   - 在编辑器中修改代码
   - 例如修改变量名或添加注释
   - 此时"恢复"按钮会变为红色边框（可点击状态）

6. **测试恢复功能**
   - 点击"恢复"按钮
   - 代码会立即恢复为原始状态
   - 输出区域会被清空

7. **测试禁用状态**
   - 不修改代码，直接观察"恢复"按钮
   - 按钮应该是灰色禁用状态

## 预期结果

### 编辑模式下
- "恢复"按钮在工具栏中显示
- 未修改代码时：灰色禁用状态
- 修改代码后：红色边框，可点击
- 点击后：代码恢复为原始状态

### 查看模式下
- "恢复"按钮隐藏
- 只显示：`[复制] [编辑] [全屏] [运行]`

### 全屏模式下
- 有独立的"重置"按钮
- 功能与"恢复"按钮相同

## 测试代码示例

### 原始代码
```python
# 计算 BMI
height = float(input('身高(cm)：'))
weight = float(input('体重(kg)：'))
bmi = weight / (height / 100) ** 2
print(f'{bmi = :.1f}')
if 18.5 <= bmi < 24:
    print('你的身材很棒！')
```

### 修改后的代码
```python
# 我的 BMI 计算器
height = float(input('请输入您的身高(cm)：'))
weight = float(input('请输入您的体重(kg)：'))
bmi = weight / (height / 100) ** 2
print(f'您的 BMI 是: {bmi:.1f}')
if 18.5 <= bmi < 24:
    print('您的身材很棒！')
else:
    print('请注意健康饮食和运动')
```

## 界面示例

### 编辑模式工具栏
```
[复制] [查看] [恢复] [全屏] [运行]
```

### 查看模式工具栏
```
[复制] [编辑] [全屏] [运行]
```

### 全屏模式工具栏
```
[运行代码] [重置] [退出全屏]
```

## 注意事项

1. 恢复操作会清空输出结果
2. 恢复后代码会立即更新到编辑器中
3. 全屏模式有独立的"重置"按钮
4. 如果代码未被修改，按钮会显示为禁用状态

## 相关功能

- **复制按钮**：复制当前代码（包括修改后的代码）
- **编辑/查看切换**：切换编辑和查看模式
- **全屏按钮**：进入全屏编辑模式
- **运行按钮**：执行 Python 代码