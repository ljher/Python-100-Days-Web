# 恢复按钮功能

## 功能说明

在代码块的工具栏中新增了"恢复"按钮，允许用户将修改过的代码恢复为原始代码。

## 使用方法

### 1. 进入编辑模式
- 点击"编辑"按钮进入代码编辑模式
- 此时工具栏会显示"恢复"按钮

### 2. 修改代码
- 在编辑器中修改代码
- "恢复"按钮会变为可点击状态（红色边框）

### 3. 恢复原始代码
- 点击"恢复"按钮
- 代码会立即恢复为原始状态
- 输出区域会被清空

### 4. 查看模式
- 点击"查看"按钮切换到查看模式
- "恢复"按钮会隐藏

## 界面变化

### 编辑模式工具栏
```
[复制] [查看] [恢复] [全屏] [运行]
```

### 查看模式工具栏
```
[复制] [编辑] [全屏] [运行]
```

### 按钮状态

| 按钮 | 状态 | 说明 |
|------|------|------|
| 恢复 | 灰色（禁用） | 代码未被修改 |
| 恢复 | 红色边框（可点击） | 代码已被修改 |
| 恢复 | 隐藏 | 非编辑模式 |

## 技术实现

### 状态管理
- `isModified`：跟踪代码是否被修改
- 当用户输入时设置为 `true`
- 当点击"恢复"或"重置"时设置为 `false`

### 触发条件
- 用户修改代码 → `isModified = true`
- 点击"恢复"按钮 → `handleReset()` → `isModified = false`
- 点击"编辑/查看"切换 → 保持 `isModified` 状态

### 按钮样式
- 禁用状态：灰色背景，灰色文字，不可点击
- 启用状态：白色背景，红色文字和边框，可点击

## 使用场景

1. **学习实验**：用户可以自由修改代码进行实验，然后恢复原始代码
2. **对比测试**：修改代码后运行，再恢复原始代码对比结果
3. **错误恢复**：当代码修改出错时，快速恢复到可工作的状态
4. **教学演示**：教师可以演示代码修改，然后恢复原状

## 注意事项

- "恢复"按钮只在编辑模式下显示
- 恢复操作会清空输出结果
- 恢复后代码会立即更新到编辑器中
- 如果代码未被修改，按钮会显示为禁用状态

## 相关功能

- **重置按钮**：在全屏编辑模式中，也有重置按钮
- **复制按钮**：复制当前代码（包括修改后的代码）
- **编辑/查看切换**：切换编辑和查看模式